import { AhfEvent } from 'domain/ahf-event/ahf-event.types';

import { State } from './initialState';

export const writeEventsReducer = (state: State, event: AhfEvent): State => {
  debugger;
  return {
    ...state,
  };
};
