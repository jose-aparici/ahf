import { makeStyles } from '@material-ui/core';

export const useParamDetailComponentStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'space-between',
  },
}));
