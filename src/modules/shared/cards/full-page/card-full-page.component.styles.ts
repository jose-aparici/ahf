import { makeStyles } from '@material-ui/core';

export const useCardFullPageComponentStyles = makeStyles(() => ({
  gridContainer: {
    minHeight: '81vh',
  },
  cardContainer: {
    minHeight: '100%',
  },
}));
