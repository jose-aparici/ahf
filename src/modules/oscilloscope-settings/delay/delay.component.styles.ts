import { makeStyles } from '@material-ui/core';

export const useDelayComponentStyles = makeStyles(() => ({
  labelContainer: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  label: {
    color: 'black',
  },
}));
