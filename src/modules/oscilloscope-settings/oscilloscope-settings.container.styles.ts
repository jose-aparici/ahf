import { makeStyles } from '@material-ui/core';

export const useOscilloscopeSettingsContainerStyles = makeStyles(() => ({
  menuPaper: {
    bottom: '55px',
  },
  label: {
    color: '#1a171b',
  },
}));
