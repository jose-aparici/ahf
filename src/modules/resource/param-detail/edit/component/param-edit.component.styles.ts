import { makeStyles } from '@material-ui/core';

export const useParamEditComponentStyles = makeStyles(() => ({
  root: {
    flex: '1',
  },
  keyboardContainer: {
    marginTop: 'auto',
  },
  inputField: {
    fontSize: '30px',
  },
}));
