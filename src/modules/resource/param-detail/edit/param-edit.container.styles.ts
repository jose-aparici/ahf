import { makeStyles } from '@material-ui/core';

export const useParamEditContainerStyles = makeStyles(() => ({
  mainGrid: {
    flex: '1',
  },
  keyboardContainer: {
    marginTop: 'auto',
  },
  inputField: {
    fontSize: '30px',
  },
}));
