import { makeStyles } from '@material-ui/core';

export const useHeaderContainerStyles = makeStyles(() => ({
  toolBar: {
    display: 'flex',
    minHeight: '0',
  },
  iconsSection: {
    marginLeft: 'auto',
  },
}));
