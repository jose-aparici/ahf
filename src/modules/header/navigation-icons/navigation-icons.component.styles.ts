import { makeStyles, Theme } from '@material-ui/core';

export const useNavigationIconsComponentStyles = makeStyles((theme: Theme) => ({
  iconButton: {
    margin: '0',
    padding: '0',
  },
  gridIcon: {
    marginRight: '16px',
  },
}));
